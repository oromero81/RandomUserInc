ext {
    androidCompileSdk_version = 32
    androidMinSdk_version = 24
    androidTargetSdk_version = 32

    androidApplicationId = "cat.oscarromero.randomuser"

    def versionMajor = 1
    def versionMinor = 0 // Maximum 99
    def versionPatch = 0 // Maximum 99
    def versionDev = 0 // Maximum 99

    androidVersionCode = versionMajor * 1000000 + versionMinor * 10000 + versionPatch * 100 + versionDev
    androidVersionName = "${versionMajor}.${versionMinor}.${versionPatch}"
}
